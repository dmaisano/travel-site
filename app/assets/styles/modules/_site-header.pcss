.site-header {
  padding: 10px 0;
  position: absolute;
  width: 100%;
  z-index: 2;
  background-color: rgba($mainBlue, .3);

  &__btn-container {
    float: right;
  }

  &--logo {
    position: absolute;
    top: 0;
    background-color: $mainBlue;
    padding: 1.5625rem 2.25rem;
  }
}
